<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="200dp"
            android:layout_height="300dp"
            android:scaleType="fitCenter"
            app:imageFromUrl="@{message.photoUrl}"
            app:isVisible="@{isPhotoAvailable}" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            app:isVisible="@{!isPhotoAvailable}">

            <TextView
                android:id="@+id/username"
                android:text="@{message.username}"
                style="@style/TextAppearance.Quiz.Caption"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/text_margin" />

            <com.google.android.material.card.MaterialCardView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/AppTheme.CardView">

                <TextView
                    android:id="@+id/message_text_view"
                    style="@style/TextAppearance.Quiz.Normal"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_margin="@dimen/text_margin"
                    android:text="@{message.text}"
                    android:textColor="@android:color/black"
                    android:textSize="20sp" />

            </com.google.android.material.card.MaterialCardView>


        </LinearLayout>
    </LinearLayout>

    <data>

        <variable
            name="message"
            type="com.example.quiz.model.Message" />

        <variable
            name="isPhotoAvailable"
            type="Boolean" />
    </data>
</layout>
